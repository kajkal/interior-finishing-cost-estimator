mutation Login($data: LoginFormData!) {
    login(data: $data) {
        accessToken
        user {
            name
            email
            products {
                name
            }
            projects {
                name
            }
            offers {
                name
            }
        }
    }
}
