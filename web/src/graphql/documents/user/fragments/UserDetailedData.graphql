fragment UserDetailedData on User {
    name
    slug
    email
    isEmailAddressConfirmed
    hidden
    avatar
    products {
        ...ProductData
    }
    projects {
       ...ProjectBasicData
    }
    inquiries {
        ...InquiryData
    }
    bookmarkedInquiries
}
