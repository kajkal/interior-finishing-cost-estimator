fragment UserDetailedData on User {
    name
    slug
    email
    products {
        ...ProductData
    }
    productCount
    projects {
       ...ProjectBasicData
    }
    offers {
        name
    }
}
