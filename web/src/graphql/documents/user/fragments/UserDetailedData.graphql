fragment UserDetailedData on User {
    name
    slug
    email
    isEmailAddressConfirmed
    hidden
    avatar
    products {
        ...ProductData
    }
    productCount
    projects {
       ...ProjectBasicData
    }
    inquiries {
        id
    }
}
