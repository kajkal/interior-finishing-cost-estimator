fragment UserDetailedData on User {
    name
    slug
    email
    avatar
    products {
        ...ProductData
    }
    productCount
    projects {
       ...ProjectBasicData
    }
    offers {
        name
    }
}
