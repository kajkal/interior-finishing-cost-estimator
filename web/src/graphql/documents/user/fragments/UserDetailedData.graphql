fragment UserDetailedData on User {
    name
    slug
    email
    isEmailAddressConfirmed
    avatar
    products {
        ...ProductData
    }
    productCount
    projects {
       ...ProjectBasicData
    }
    offers {
        name
    }
}
