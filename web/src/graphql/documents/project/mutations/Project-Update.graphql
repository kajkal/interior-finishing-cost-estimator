mutation UpdateProject(
    $projectSlug: String!
    $name: String!
    $location: LocationFormData
) {
    updateProject(
        projectSlug: $projectSlug
        name: $name
        location: $location
    ) {
        ...ProjectDetailedData
    }
}
