mutation Register($data: RegisterFormData!) {
    register(data: $data) {
        name
        email
        products {
            name
        }
        projects {
            name
        }
        offers {
            name
        }
    }
}
