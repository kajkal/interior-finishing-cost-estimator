mutation Register($data: RegisterFormData!) {
    register(data: $data) {
        accessToken
        user {
            name
            email
            products {
                name
            }
            projects {
                name
            }
            offers {
                name
            }
        }
    }
}
