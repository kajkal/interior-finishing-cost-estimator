mutation Login($email: String!, $password: String!) {
    login(email: $email, password: $password) {
        accessToken
        user {
            name
            slug
            email
            products {
                name
            }
            productCount
            projects {
                id
                name
                slug
            }
            offers {
                name
            }
        }
    }
}
