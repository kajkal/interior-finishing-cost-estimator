import React from 'react';


export interface UserProfilePageProps {
    // children: React.ReactNode;
}

export function UserProfilePage(props: UserProfilePageProps): React.ReactElement {
    return (
        <div>
            UserProfilePage
        </div>
    );
}
