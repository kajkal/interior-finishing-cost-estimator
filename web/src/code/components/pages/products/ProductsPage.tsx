import React from 'react';


export interface ProductsPageProps {
    // children: React.ReactNode;
}

export function ProductsPage(props: ProductsPageProps): React.ReactElement {
    return (
        <div>
            ProductsPage
        </div>
    );
}
